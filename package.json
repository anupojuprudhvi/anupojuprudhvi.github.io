{
  "name": "prudhvi-portfolio",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node scripts/verify.mjs",
    "preview": "node scripts/serve.mjs",
    "check": "npm run build && git diff --exit-code --stat"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "playwright": "^1.55.0"
  }
}
